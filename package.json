{
  "name": "bura_to",
  "version": "0.0.1",
  "description": "Personal Landing Page based on Identity by HTML5 UP",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aleburato/bura.to.git"
  },
  "author": {
    "name": "Alessandro Burato",
    "email": "ale@bura.to"
  },
  "dependencies": {
    "gatsby": "^2.29.1",
    "gatsby-plugin-manifest": "^2.9.0",
    "gatsby-plugin-offline": "^3.7.0",
    "gatsby-plugin-react-helmet": "^3.7.0",
    "gatsby-plugin-sass": "^2.8.0",
    "node-sass": "^4.13.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-helmet": "^6.1.0"
  },
  "scripts": {
    "start": "gatsby develop",
    "build": "npm run clean && gatsby build",
    "deploy": "npm run clean && gatsby build --prefix-paths && gh-pages -d public",
    "serve": "gatsby serve",
    "clean": "rimraf .cache public",
    "format": "prettier --write src/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  },
  "keywords": [
    "gatsby",
    "gatsby-starter",
    "gatsby-starter-identity"
  ],
  "license": "MIT",
  "homepage": "https://bura.to"
}
